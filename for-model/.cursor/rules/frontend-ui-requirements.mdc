---
description: Defines overall flow and requirements for Frontend UI. Provides user experience (UX) and interface design guidelines.
alwaysApply: false
---
Please configure the Frontend UI based on the following UI requirements.

{{#if boundedContexts}}
{{#boundedContexts}}
{{#if aggregates}}
{{else}}
{{#editDesc description}}{{/editDesc}}
{{/if}}
{{/boundedContexts}}
{{/if}}

<function>
window.$HandleBars.registerHelper('editDesc', function (desc) {
    // Function to remove all quotes (", ')
    if (typeof desc === 'string') {
        return desc.replace(/['"]/g, '');
    }
    return desc;
})
</function>